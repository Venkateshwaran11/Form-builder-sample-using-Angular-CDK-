name: Angular CI

on: pull_request:
  branches: -main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
    with:
    node-version: '24'
    cache: 'npm'

    - name: Install dependencies
    run: npm ci

    - name: Build Angular application
    run: npm run build

    - name: Run unit tests
    run: npm test -- --watch=false --browsers=ChromeHeadlessCI --no-progress --browsers=ChromeHeadlessCI